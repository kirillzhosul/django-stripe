# Web framework (Django).
django==4.1.1
django-environ==0.9.0

# Other.
stripe==4.1.0  # Stripe SDK.

# Database.
psycopg2-binary==2.9.3  # SQL Engine (PostgreSQL engine core).

# ASGI server worker with
# server process manager.
uvicorn==0.18.3
gunicorn==20.1.0
