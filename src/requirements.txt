# Django stripe.

# Web framework (Django).
django==4.1.3
django-environ==0.9.0

# Other.
stripe==4.2.0  # Stripe SDK for payments.

# Database.
psycopg2-binary==2.9.5  # SQL Engine (PostgreSQL engine core).

# ASGI server worker with
# server process manager.
uvicorn==0.19.0
gunicorn==20.1.0
